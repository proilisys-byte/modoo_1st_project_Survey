/** 현재 설문 배포 버전 — 신규 응답에 저장 (v1 응답은 수정하지 않음) */
export const SURVEY_VERSION = "v2";

/** 채점·등급 설정 버전 — config/grade_bands.json version 과 동기화 */
export { GRADE_CONFIG_VERSION as SCORING_CONFIG_VERSION } from "./grade-bands";
